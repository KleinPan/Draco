<UserControl x:Class="UniversalFwForWPF.SettingUc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UniversalFwForWPF"
              xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="auto" />

        </Grid.ColumnDefinitions>
        <Button Name="ButtonConfig" Grid.Column="1"
                hc:IconElement.Geometry="{StaticResource ConfigGeometry}"
                Click="ButtonConfig_OnClick"
                Foreground="{StaticResource PrimaryTextBrush}"
                Style="{StaticResource ButtonIcon}" />

        <Popup Name="PopupConfig" Grid.Column="1" PlacementTarget="{Binding ElementName=ButtonConfig}" StaysOpen="False">

            <StackPanel>
                <Button>语言</Button>
            </StackPanel>

        </Popup>
    </Grid>
</UserControl>
